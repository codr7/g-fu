* wrap try around repl input
* allow passing restart args from break loop
** parse args after n
** update readme example
* --
* move (iter) to SeqType
** update lib/tests
* move bench/find-key/test to lib
* move new-sym to Sym/new
* add switch val-arg
** inject into vec-conds if != _
